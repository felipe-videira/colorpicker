import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerContainer: {
    flexDirection: "row"
  },
  headerText: {
    fontSize: 50,
    color: "#ecf0f1",
    fontFamily: "mainFont",
  },
});
